#include <iostream>
#include <vector>

using namespace std;

/* Read an unlimited number of ints with no attempts at error
recovery; fill the vector parameter with the data; its size
after the return tells how many items were read.
*/
void getInts ( vecotr<int> & array )
{

    int itemsRead = 0;
    int inputVal;
}
